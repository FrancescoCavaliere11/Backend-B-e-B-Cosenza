[pytest]
# I test dello Step B sono puri: validano gli schemi Pydantic senza toccare
# database o rete. L'harness asincrono con database di test arriverà allo
# Step C, dove il test di concorrenza lo rende indispensabile.
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
